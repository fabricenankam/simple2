pipeline {
    agent any 
    stages {
        stage('Stage 1') {
            steps {
                 script{
                withSonarQubeEnv(credentialsId: 'Sonarqube') {
          sh 'mvn clean install sonar:sonar'
             }
                } 
            }
        }
    }
}