pipeline {
    agent any 
    stages {
        stage('Stage 1') {
            steps {
                 script{
                withSonarQubeEnv(installationName: 'Sonarqube-8.9.10') {
          sh 'mvn clean install sonar:sonar'
             }
                } 
            }
        }
    }
}